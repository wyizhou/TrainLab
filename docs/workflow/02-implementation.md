# 阶段二：实现

## 目标

在规划阶段确定的范围内直接完成代码和测试。主 Agent 承担 Executor 职责和唯一写入责任，不通过旧 harness 状态文件取件。

## 工作原则

- 先理解现有组件和数据流，再修改；优先扩展现有实现。
- 保持改动聚焦，不顺带重构无关区域。
- 保留用户已有修改，不使用破坏性命令清空工作区。
- 行为修改同步补单测；用户旅程、响应式和视觉契约修改同步补 e2e。
- UI 实现同时检查整页结构和局部 computed 样式，不能只让单个锚点通过。
- 继续集中使用设计 token，并保持 `data-vc` 锚点稳定。
- 设计导出是输入，不通过修改当前权威设计基线来迁就实现。

## 完成条件

进入验收阶段前应满足：

- 规划范围内的功能已实现。
- 相关测试已新增或更新。
- 没有临时文件、调试输出或无关格式化改动。
- 已知限制和未完成项能够明确说明。
