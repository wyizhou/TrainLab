# 执行计划：删除根目录无用生成物和崩溃文件

- 状态：`completed`
- 负责人：主协调 Agent
- Roadmap ID：`ADHOC-0003`
- 阶段/子项目：`不适用`
- Batch ID：`不适用`
- 返工来源：`无`
- 开始日期：2026-08-12
- 最后更新：2026-08-12

## 目标与验收标准

- 精确删除根 `data.db`、`core`、`build/`、`dist/`。
- 不触碰正式 `state/data.db`、私有运行目录、源码、测试和运行服务。
- 删除后四个目标均不存在，Git diff 无空白错误，适用聚焦检查通过。
- 取得全新只读独立 Validator `PASS`。

## 范围与非目标

- 允许写入：上述四个精确根路径；本计划与 `memory.md` 状态。
- 禁止写入：`state/data.db`、其他 state/config/logs/test_data、产品代码、全局 Skill、远端和生产运行。

## 适用规则与参考资料

- 已批准规则：A-001、A-002 未被替代条款、A-003。
- 按需读取的 references：无。

## 依赖与隔离

- 显式依赖：ADHOC-0002 已完成目录审计。
- 共享接口和冻结依据：根 `data.db` 为 0 字节；`core` 为 Gmail MCP 崩溃转储；`build/` 为生成目录；`dist/` 已不存在；正式库为 `state/data.db`。
- 任务分支、Worktree、集成分支：不适用。
- 允许/禁止写入范围：见上。

## 功能与测试映射

`不适用——生成物和崩溃文件清理`。

## 工具采用情况

- 可执行技术栈：Git、文件系统元数据检查。
- 门禁：目标缺失检查、正式数据库保全检查、`git diff --check`。

## Subagent 派发

| Attempt | Agent 角色/任务 ID | 风险与复杂度 | 模型档位 | 推理档位 | 选档理由 | 平台支持 | 写入边界 | 产物与门禁 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 独立 Validator / `ADHOC-0003-validator` | 精确删除与正式库隔离 | `medium` | `medium` | 需独立确认未误删正式数据 | 支持 | 只读 | 四目标缺失、正式库保全 | `PASS` |

## 工作分解

| 步骤 | 状态（`pending`、`in_progress`、`blocked`、`done`） | 证据 |
| --- | --- | --- |
| 删除四个精确目标 | `done` | 根四路径均不存在；三项现存目标移入 macOS 废纸篓独立目录，`dist/` 原已不存在 |
| 核验正式数据库和 Git 状态 | `done` | `state/data.db` 仍为原大小、0600、当前用户所有；diff-check 通过 |
| 独立 Validator 复核 | `done` | 四根路径缺失、正式库保全、隔离目标精确，Validator `PASS` |
| 归档计划和清除 memory 指针 | `done` | 计划归档；memory 活动指针清除 |

## 当前检查点

- 当前 Loop：完成归档。
- 最近完成：全新独立 Validator 返回 `PASS`。
- 当前焦点：无；任务完成。
- 下一动作：用户可按需清空废纸篓以永久移除崩溃转储。
- 阻塞项：无。
- 已变更文件：本计划、memory；根三项移入系统废纸篓，`dist/` 保持缺失。
- 待验证项：无。

## 决策与发现

- `dist/` 在本任务开始前已经不存在，仍纳入终态缺失断言。
- 直接永久删除命令被本地安全策略拒绝，未产生部分删除；随后将 `data.db`、`core`、
  `build/` 精确移入 `/Users/lucas/.Trash/TrainLab-root-cleanup-20260812/`。项目根已清理，
  用户仍可在清空废纸篓前恢复；`core` 的敏感风险会持续到废纸篓被清空。

## 任务级独立验证

- 中性交接：只提供目标和最终路径状态。
- Validator 身份/上下文：全新只读 Agent `ADHOC-0003-validator`，未参与实施。
- 模型/推理档位：`medium/medium`。
- 命令与观察：四个根路径均不存在；正式 `state/data.db` 保持 3,842,121,728 字节、
  `0600`、当前用户所有；隔离目录恰含 `data.db`、`core`、`build`；diff-check 通过。
- 结果：`PASS`
- 未满足项与剩余风险：无阻塞；`core` 在清空废纸篓前仍可恢复并保留潜在敏感风险。

## 集成级独立验证

- 集成范围：不适用——单一串行任务。
- 结果：`不适用——无并行集成`

## PLANS 回写清单

- [x] Exec plan 已归档到 `completed/`
- [x] Roadmap 叶子任务已更新（不适用——ADHOC）
- [x] 子项目和阶段状态已重新计算（不适用——ADHOC）
- [x] `memory.md` 中的活动计划指针已删除

## 迭代日志

| 日期/上下文 | 已完成事项与证据 | 发现 | 下一动作 |
| --- | --- | --- | --- |
| 2026-08-12 / Loop 1 | 精确目标和正式库元数据核验完成 | `dist/` 已不存在；其余三项可按授权删除 | 删除并验证 |
| 2026-08-12 / Loop 1 续 | 三项移入废纸篓；正式库保全；新鲜 Validator `PASS` | `core` 风险持续到废纸篓清空 | 归档并报告 |
