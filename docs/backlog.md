# Backlog — 全局待办视图

本文件只做**全局视图与执行排序**，不承载单需求细节。
单需求细节在 `docs/executor/active/` 内，一条一个文件，全部套用 `docs/executor/template.md`。

由 Planner 维护。Executor / Validator 只读。

---

## 执行顺序

按序号字典序执行（`003` < `003b` < `004`）。墓碑（`SUPERSEDED-*`）不参与执行。

| 序号 | 状态 | source_design_rev | 分支 | 摘要 |
|---|---|---|---|---|
| _（暂无需求）_ | | | | |

---

## 依赖排序说明

若某新增需求必须在已有需求之前执行，先在此显式记录依赖关系，再决定编号。
新增需求默认取当前最大序号 + 1，不插队。
