# BASE-00 可复现开发基线

状态：已建立；本报告只固定已有实现，不代表任何真实环境验收或生产切换

## 标识与范围

- `BASELINE_ID`：`trainlab-base-00-2026-07-24`
- 父提交：`629053fdc32b87adfe7eb0e2907f310a0f3ed958`
- 建立日期：2026-07-24（Asia/Singapore）
- 目标：冻结第一层、L2-01～12、A3-01～10、M4-01～08、S5-00～07 的当前实现和
  离线验证证据。
- 本任务未实现 L2-13 或任何后续单元；未调用 Garmin、Gmail、Codex 分析、生产数据库
  或部署服务。

本次提交本身是可复现快照的权威引用；后续 worktree 必须从该提交创建，并在交付回执中
记录本 `BASELINE_ID`。

## 冻结契约核验

| 契约 | SHA-256 | 结果 |
|---|---|---|
| 第一层 v2.4 | `9bf0a91e61bda47c9dba971c731ca6ec79e064b7f0ea4eb8124a5a216d67e396` | 匹配 |
| 第二层 v1 | `afdffa6268d64403d170906ee6a42d10f873a75b1615b806d893982aea86db14` | 匹配 |
| 第三层 v2 | `4a9b3f1ffb92ba1ad4f56e68380344007e48a43e03a3e0f746399c50da794eb8` | 匹配 |
| 第四层 v2 | `47175280f917dee55d2aa5f663f0705da8ec7baf463eae5933ea391c0770be94` | 匹配 |
| 第五层 v1 | `5b1d5abf15689cec2507bd20e36aa83fe49626dd82247a4c0a1b91cc266ce804` | 匹配 |

## 纳入范围

- 已修改的项目入口、依赖声明和生产 Harness：`README.md`、`pyproject.toml`、
  `requirements.lock`、`harness/shared/`、`harness/runtime/`、`src/trainlab/cli.py`、
  `src/trainlab/mcp.py`。
- 第一层实现、初始化脚本、Schema、运行手册、数据基础层契约与验收测试。
- 第二层 Garmin 采集实现、资源目录、客户端、配置/Schema、脱敏 JSON fixture 和
  L2-01～12 离线测试。
- 第三层分析实现、production analysis Harness、Schema、策略和 A3-01～10 离线测试。
- 第四层 Mail Agent 实现、production mail Harness、Schema 和 M4-01～08 离线测试。
- 第五层 orchestration 实现、Schema 和 S5-00～07 离线测试。
- 五层大契约、开发清单、总控编排方案及此基线报告。

共享文件所有权保持总控方案定义：Foundation DDL 仅第一层维护；Garmin、Analysis、Mail
Agent、Orchestration 分别维护各自目录；共享 CLI/MCP、依赖锁、shared/runtime Harness 和
接口 manifest 仅在总控集成任务中继续变更。

## 排除范围

| 路径或类别 | 原因 | 提交状态 |
|---|---|---|
| `.coverage` | 测试运行产物 | 排除 |
| `config/foundation.yaml`、`config/garmin.yaml` | 可能为环境专属配置；不将配置/凭据风险纳入代码基线 | 排除 |
| `test_data/**` | 用户提供的代表性 FIT；manifest 明确不保证匿名化 | Git 忽略，不提交 |
| `__pycache__/`、虚拟环境、pytest 临时目录 | 生成物 | Git 忽略，不提交 |
| token、密码、授权 URL、健康原始 JSON、邮件正文 | 未读取、未记录 | 不得进入提交 |

为运行离线测试，六个 FIT 仅从主工作区复制到当前 worktree 的 `test_data/new/`。它们仍由
`.gitignore` 第 13 行忽略，且已按 `tests/fixtures/garmin_fit_manifest.json` 校验 SHA-256：
全部匹配。复制文件不属于本提交。

## BASE-00A｜私有数据跟踪修正

本基线的前一提交曾跟踪 `source/` 和 `test_data/` 下的 10 个私有健康文件。BASE-00A
仅使用 `git rm --cached` 将这 10 个路径从新的基线 tip 移除：本地文件保留，未读取其内容，
也没有重写历史。

- 当前基线 tip 不再包含 `source/**` 或 `test_data/**` 路径。
- 六个代表性 FIT 只保留在本地 `test_data/new/` 供离线测试使用，继续受 `.gitignore`
  保护，绝不提交。
- 旧提交历史仍可能包含这些文件；若需清理本地或远端历史，必须由用户另行明确授权，
  并在独立的历史重写任务中完成。

## BASE-00B｜数据库跟踪修正

BASE-00B 仅将根目录 `data.db` 从 Git 索引移除。本任务没有打开、查询、导出或修改
数据库内容；本地文件保持原样，且继续由现有 `.gitignore` 规则忽略。

- 当前基线 tip 不再包含 `data.db`，也不包含任何 `.db`、`.sqlite`、`.sqlite3`、WAL、
  SHM 或 journal 数据库路径。
- 旧 Git 历史仍可能包含该数据库。若需清理本地或远端历史，必须经用户另行明确授权，
  在独立的历史重写任务中进行。

## 验证记录

所有测试均在当前 worktree 的 `src/` 上运行，使用已有 Python 3.12 虚拟环境；没有网络或
外部 provider 调用。

| 范围 | 命令 | 结果 |
|---|---|---|
| 第一层（预检） | `pytest -q tests/test_foundation*.py` | 通过 |
| 第二层 L2-01～12 | `pytest -q tests/test_garmin*.py` | 通过 |
| 第三层 A3-01～10 | `pytest -q tests/test_analysis_a3_*.py` | 通过 |
| 第四层 M4-01～08 | `pytest -q tests/test_mail_agent*.py` | 通过 |
| 第五层 S5-00～07 | `pytest -q tests/test_orchestration_s5_*.py` | 通过 |
| 静态 | `python -m compileall -q src/trainlab` | 通过 |
| 静态 | `git diff --check` | 通过 |

执行规则为每个层级只运行一次；本次没有失败、修复或重跑。

暂存内容的凭据特征扫描只命中两种预期文本：`mail_agent/context.py` 中用于检测而非保存
Google secret 的正则，以及 `test_mail_agent_m4_06.py` 中明确标注为 `synthetic` 的脱敏
测试语料。两者均非真实凭据，且环境配置和真实 token 均未纳入提交。

## 当前完成状态与后续边界

| 层 | 当前已固定范围 | 不在本基线内的后续工作 |
|---|---|---|
| 第一层 | FND-01～13 及返修单元 | 仅显式维护/migrate；不再作为日常服务开发 |
| 第二层 | L2-01～12 | 从 L2-13 开始 |
| 第三层 | A3-01～10 | 从 A3-11 开始 |
| 第四层 | M4-01～08 | 从 M4-09 开始 |
| 第五层 | S5-00～07 | 从 S5-11 开始；S5-11 先于业务 workflow |

在本提交存在且总控验收通过后，可以创建只负责 L2-13 的新 worktree。该单元不得修改
共享 CLI/MCP、依赖锁、shared/runtime Harness、Foundation DDL，且不得开始 L2-14 或
其他层单元。
