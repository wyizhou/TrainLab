# 数据归档与回滚

迁移前先停止 TrainLab 自身的写进程，记录非内容元数据；旧实例通过同文件系统原子
重命名保存到 `data-backup/<timestamp>/`。归档目录完全被 Git 忽略，不由产品入口读取。

新库候选验证失败时，只移除候选路径并保留失败证据；旧归档不覆盖、不删除。只有候选
通过完整性、外键、哈希、权限和只读查询后，才原子重命名为 `source/state/`。
