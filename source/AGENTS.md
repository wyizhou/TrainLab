# TrainLab 产品源码快照边界

2026-09-15：本目录仅保存来自 TrainLab-next 的未验证源码快照，供后续直接 API 重构参考。
本次不验证或修复旧代码，不实现 API，不启动现存 Agent runner、旧 R7、daemon 或业务服务。
开发指令、私人资料与外部动作边界以主根 [AGENTS.md](../AGENTS.md)、[rules.md](../rules.md)
和本次 [ADHOC-0019](../docs/exec-plans/active/ADHOC-0019-adopt-next-before-api.md) 为准。
未来工程检查规则并未取消；用户本轮免验仅针对本次旧代码快照保存。

以下完整保留来源文件作为历史说明。其中“已验收”“已恢复”、next 的 PLAN/exec-plans 路径
及治理替代文字，不是主目录当前状态、开发规则或新的执行授权，不按下文恢复旧业务。

---

# TrainLab 产品边界：FIT-only 每周系统（来源原文）

旧运行入口已停用并从活动工程退出。M12 的 Python 统一入口已通过 R6 完整独立离线验收和 Main 实际验收，真实服务验收尚未完成；不要因读取本文件而执行旧每日健康、
日报 AI、M7–M11 固定测试批次或任何外部动作。开发协作统一遵循根 [AGENTS.md](../AGENTS.md)，
当前产品要求与验收依据见 [现行产品合同](../docs/product-contract.md)，大功能总览见 [PLAN.md](../PLAN.md)，暂停条件与恢复位置见 [M12 执行计划](../exec-plans/active/M12-fit-weekly.md)。
RUN-VC-001 的产品验收条款由现行产品合同继承，开发协作与交付条款已按本次批准由根 AGENTS.md 替代；合同中保留的 A-021、A-022、A-023 等产品约束继续适用。
A-023 要求新方案同批替代受影响旧实现和验收，不拼接历史合同，不改变另一开发路线。
当前命令和私有空模板见[统一入口](docs/fit-weekly-entrypoint.md)，后续任务以关联 M12 执行计划为准。用户已明确恢复业务开发与本地检查；当前任务及后续暂停/恢复条件以M12执行计划为准。
合成离线检查不能证明真实CLI、Provider或正式实例已经验收。

## 新方向与运行职责

- source/ 仍是唯一产品工程。Python 负责 FIT 同步、解析、SQLite、调度与动作账本。
- 每周先独立跑步规划 `plan`，后全运动总结 `summary`，两次独立无状态 AI 上下文；各阶段
  最多调用一次，失败/unknown 不自动模型重试，不依赖聊天历史或 resume，不做日报或健康采集。
- 规划只读本期跑步、最多四份历史中可验证跑步分析/计划、用户目标/限制/可训练时间和跑步细读；
  排课不读取非跑步活动或混合历史全文，无独立跑步字段的历史不整篇传入。固定七日跑步计划
  只依据跑步与目标，不因非跑步负荷调整；硬负荷最多 3 次且日期差至少 3 天。
- 总结读取本期全部运动、最多四份完整成功 M12 历史、目标及本次已验证计划；不修改或替代
  计划，不用 M11 报告充数。两阶段合并为一份周报及唯一计划，目标与原始周输入快照不改写。
- 两阶段合计共享每周最多 20 次细读、每次最多 20 分钟，同请求缓存；规划仅能细读跑步。
  每阶段输入/intent/结果独立，监督与耐久恢复复用一套实现；重启/版本/搬迁不重获额度。
  规划失败不启动总结，总结失败保留计划但不发布，成功阶段不重跑；旧单阶段只读兼容不产生新授权。
- 运动表现不是健康诊断。计划保留 RPE、明确步骤、技术备注和停止条件；不推算心率区间、
  阈值或目标 BPM。设备记录的历史心率及分区时长可展示，不用于生成区间处方。
- 所有模型输入由确定性投影生成；按 A-022 允许运动 GPS、经纬度、路线、地点与活动名称，
  保留来源/时间/SHA；不提供原始 FIT 字节、无关个人/设备标识、凭据、任意路径或 SQL。
  细读经过同一边界，位置缺失不丢弃活动；旧快照按保存版本读取，不自动补写或重跑。
- 邮件使用 Gmail REST 的基础发送与本地编辑；PDF 和 Garmin 课程来自同一已验证结果。
  不回退 Gmail MCP、SMTP、Connector 或旧 M10/M11 批次。
- 日常同步目标为香港时间 22:00，周任务为星期日 15:00；调度与补发由 Python 管理，
  不安装 cron、登录启动项或开机服务。本段是已批准目标，不表示调度已经运行。

## 运行与历史边界

- state/ 的旧库、raw 和现有私人配置原样保留；本阶段不切换正式数据。
- data-backup/ 是可删除的旧系统归档，绝不能作为新系统的运行依赖。
- 必要公共能力先提取和验证；已取消业务的旧脚本/Schema/测试已逐项映射后退出活动路径，不作自动路线或永久质量门。
- Schema注册与模型运行标识只使用当前明确依赖，不能扫描全部历史目录；归档不存在时仍须正常运行。
- 旧脚本中的历史授权、固定日期或 prepared 动作都不能授权新的 Provider 调用。
- 新模块离线测试、独立验证和实际前置检查完成后，才能按当前精确在线授权运行。
- 开发协作、失败处理、本地验收与未来 PR 流程统一遵循根 [AGENTS.md](../AGENTS.md)，
  产品详细进度只记录在 [M12 执行计划](../exec-plans/active/M12-fit-weekly.md)，[PLAN.md](../PLAN.md) 只同步总览。
  本地检查不主动触发远端 CI，也不授权真实模型、同步、邮件或 Garmin 操作。
- 私有 Goal.md（含旧 goal.md）、Email.md、OAuth Client、Token 不进 Git，也不复制进备份或 AI job。
- 正常运行不得读取 tests/；测试只用合成输入或明确批准的隔离实例。

旧规则与测试的迁移说明见 [退役映射](docs/legacy-retirement.md)。
