# 变更日志

本文件记录 TrainLab 的重要变更。普通工作归入 `Unreleased`；只有用户明确要求发布并批准
版本号后，才能整理为正式版本。

## Unreleased

- 2026-09-15：按用户要求保存 TrainLab-next 实际公开产品树为 API 重构前的未验证源码快照，保留主开发 Harness 和既有 CI；本次不验证旧代码、不追补历史校验记录，不宣称 M12 可运行或旧失败已修复。后续转向直接 API 接入，不再依赖其他产品的 Agent 运行；现存 runner 尚未替换，本次未实现 API、启动业务或切换私人实例。Goal、FIT、认证及数据库不进入 Git。

- M12 关闭 37 个旧日报、健康、固定批次、邮件及渲染命令，保留 9 个当前或显式维护入口；新增逐项入口清单与真实子进程退役测试。已取消 CLI 预期按 VC-005 退出，暂留功能的安全断言不变，剩余实现随对应模块迁移后归档。

- M12 按用户批准 VC-005/A-023 开始新旧系统收敛：当前规则与合同完整展示，保护未提交工作及历史证据；Schema 改为显式根及递归引用，模型运行标识改为实际资源闭包。必要旧安全场景迁移、业务归档与完整独立验收仍在进行，不宣称旧 M9 失败已修复或新系统已交付。

- M12 跨平台回归补齐旧 Candidate 的 Linux 原生原子交换，保留 Darwin、回滚与失败关闭行为；不恢复旧运行路线，不修改旧回执 SHA。旧 M9 退出故障注入补全 wait/poll 两条观测，正确断言保留，并增加已确认退出的对照。

- M12 按 A-022/VC-004 接通运动位置与活动名称：新版 FIT/细读/周输入保留实际定位与来源，名称仅取已保存且绑定的 inventory capture；旧快照按原版本重放。凭据、文件能力、预算与单次外部动作边界不变，旧地理拒绝失败及测试按版本保留。当前尚待完整独立验收，不表示真实周发布已完成。

- M12 新增每周固定输入和完整周报历史：保存当期全部FIT摘要/分段、目标快照与最多四份完整已验证报告；模型前复验，编辑目标或补入历史不改变已冻结周，搬迁及重放不重复模型调用。训练结果、PDF和发布仍待接入。

- M12 原 VC-003 输入检查曾要求拒绝明确路线坐标、原始载荷、路径及身份/认证资料；运动位置拒绝现由 A-022 替代，其余边界保持。首次冻结、旧快照及模型前检共用检查，不清洗历史或回写规范化文本，旧失败证据保留。

- M12 接通Codex内部薄适配器：冻结输入/配置与公开能力证据，复用原有隔离、进程监督、capture和恢复账本；新增仅连接本机假服务的公开CLI诊断，不启用真实周报或业务服务。

- M12 将完整已停止的Codex原始capture接回原周任务账本：共享输入与结果校验、无新intent/模型调用、有限落盘失败后本地补账；保留未知与历史失败，不增加任意结果文件或重试入口。

- M12 增加 Host 子进程说明/Skill 隔离组件：macOS 读取限制与 Linux 只读挂载参数、原认证位置核对、合成子进程及有限持久化回归；不修改全局配置、不安装依赖，也不单独授权私人模型运行。

- M12 新增模型进程原始证据同步保存与零启动恢复；启动前持久化绑定，完整字节/长度/SHA闭合后才返回，停止不明或落盘未完成保持非终态。仅内部Host组件，完整Codex隔离接线仍待交付。

- M12 新增业务Schema派生的Codex响应合同和单轮JSONL结果检查，区分进程、协议、模型明确失败及业务格式错误；保留原始内容并拒绝用较早JSON掩盖失败，完整Launcher仍待接入。

- M12 新增同步、有界的模型子进程监督：完整输入/输出传输、新session内所有进程组终止确认及未终态恢复；公开CLI假端点验证不调用真实模型，完整启动隔离与业务适配仍待接入。

- M12 新增一次性模型任务账本及显式 Fake adapter；intent先提交、模型阶段不占新库写锁、完整capture可本地补账，同周中断/失败不自动重跑。完整Codex启动和周内容合同仍待接入。

- M12 新增无损 FIT 列式传输及公开模型工具正文核对；原始细读事实/缓存、默认 SDK 返回和预算保持不变，完整 Launcher 尚待接入。

- M12：新增仅对单次Codex任务生效的能力配置和公开离线请求检查器；拒绝额外工具、用户说明、Skill清单及输入漂移。实际CLI合成诊断不连接真实模型；完整跨平台Launcher与私人周任务仍未交付。

- M12：将预绑定FIT细读接成单一stdio MCP工具，沿用新库预算/缓存；真实SDK合成验证参数、错误脱敏和来源绑定。未启用Codex、全局MCP配置或在线任务，模型客户端能力仍待独立验收。

- M12：新增完整周运动证据冻结，基于已完成同步与所有登记FIT按结束时间纳入全部运动；无FIT单独披露、来源和SHA闭合、迟到数据不改写冻结周。尚未接入目标/历史周报或真实AI。

- M12：新增预绑定周活动范围的本地FIT细读接口；20次/每次20分钟预算跨重启保留，摘要/圈段/细粒度统计共用白名单解析，精确请求缓存与失败回执落新库。尚未向模型开放工具或启用周任务。

- M12：新增FIT-only解析与版本化摘要/圈段/分段落库；按有效时间加权，分别披露暂停、采样缺口和距离覆盖，保留设备session分区事实并排除私人字段。仅内部离线模块，未接入AI、邮件或正式实例。

- M12：串联FIT-only分页、独立MCP会话、私有capture及新库；跨重启保留预算，闭合结果本地恢复，明确无FIT与无活动分开，整体回执必须等待会话审计。仍仅离线合成验收，未启用真实同步、邮件或调度。

- M12：新增离线测试的FIT-only Garmin MCP协议适配器；保留固定缓存认证保护，只暴露活动分页/FIT下载，区分空页、明确无FIT与错误，验证原字节/CRC/权限；尚未连接真实Garmin或启用同步编排。

- M12：新增香港时间同步日历、完整分页及跨重启调用预算账本；新库v1→v2显式事务升级，查询完成与FIT下载完成分开，尚未启用MCP采集或调度。

- M12：新增从已验证旧归档一次性导入登记 FIT 的工具；新实例独立保存原字节与身份/SHA，不继承旧健康库和外部动作，历史查询覆盖仍由后续同步模块证明。

- 新增 M12 独立私有 FIT 存储基础：可搬迁实例、单写者锁、CRC/SHA/活动绑定、不可变解析及输出、精确重放和孤立 FIT 恢复；不依赖旧账本或归档，尚未接入真实历史同步和新运行命令。

- M12 完成只读旧系统归档工具及独立恢复验证；停止旧 AI + Skills auto 入口，新增 A-021 和逐文件测试迁移清单。Gmail/Garmin 通用安全能力待迁移前原样保留，未切换正式 state、删除旧数据或启用新运行层。

- 记录 M12 新方向：FIT-only、每周跑步教练、PDF 与既有 Gmail/Garmin 发布能力；旧 M11 未完成工作仅作为检查点保留，不宣称闭环成功。按用户新增授权，每个小功能/模块通过测试与独立验证后提交并推送；本次尚未切换数据或运行新产品流程。
- 适配 agentForge main `9964970` 的开发治理简化（v0.4.4 之后、非新正式版本）：根入口与详细协议分离，按任务影响及角色加载流程，模板改为必填核心加按需章节；保留独立验证与失败诊断，新增受审快照及纯协调回写免重验边界。TrainLab 产品、私人数据和既有 M11 工作不变。
- 收敛 M11 content-first v4 的模型输出合同：模型只输出七日决策槽位，Host确定性注入周期、
  日期和成功状态；跑步/攀岩课程使用四个必填步骤对象，休息仅使用检查清单。业务Schema确定性
  投影为Structured Outputs wire；新增由业务/Host Schema确定性派生的Prompt规范语义块，自动
  校验根字段、七个槽位、课程类型、步骤和Host字段三层同构。历史RHR及活动平均/最高心率仅由
  Reader从已验证证据展示；新增Candidate/intent/receipt v3和只接受Candidate内结果的Finalizer，
  不改变旧失败证据。
- 将根开发 Harness 从 agentForge `v0.4.2` 三方适配到 `v0.4.4`：新增每任务冻结验证合同、
  Validator 固定输出、`FAIL`/`INCONCLUSIVE` 边界、反复失败诊断门和只读 Failure Analyst；
  保留 TrainLab 的 `source/` 产品布局、隐私与外部动作边界，未修改产品实现或正式 state。
- 完成M11 v3八封真实Gmail投递：用户一次性批准发送2026-08-12至18的7份日报和1份周报；
  官方REST单写者严格串行完成32次API调用和8次send，8个Gmail ID、实际Message-ID、RAW、
  text、HTML与CID全部闭合，零调用重放和独立Delivery/Data-Privacy Validator均PASS；未重跑
  Garmin/AI、未修改正式state，也未授权部署或定时运行。
- 完成 M11 OpenDesign v3 离线闭环：冻结既有AI日报、周报与课表，新增有界展示证据、共享
  Gmail table/CID渲染引擎、品牌与状态图形、香港时间、中文图表降级、真实活动/睡眠/七日趋势图
  和详细计划时间轴。历史心率分区仅展示Garmin/FIT session已记录时长，不从采样重新划区，也不
  用于课程处方。最终594项测试及Code、Design-Fidelity、Data/Privacy三类Validator全部PASS；
  未发送邮件、未调用Provider、未修改正式state。
- 启动 M11 Coaching Utility v2 内容返工：日报区分周计划原课与今日调整，周报增加健康/运动负荷
  总结、观察→意义→行动洞察和七天可执行课表；课程以 RPE/体感为主并补齐 SOS、步骤、开始门、
  降级和停止条件。TrainLab 不计算心率区间或目标 BPM，本批次只做离线 Candidate 验收。
- 收敛 M11 content-first v4 的 VC-006 模型输入边界：私人目标只按权威公开模板的1个标题、
  5个章节和19个有序字段解析，强度说明不进入模型；有限隐私词法明确活动名称、坐标、邮箱、
  文件名与斜线边界；Prompt和目标模板均绑定仓库权威SHA，输入或pending持久化失败时模型调用为0。

- 启动 M11 人类可读邮件展示：引入版本化日报/周报 ViewModel、显式字段映射、确定性静态 PNG、
  私有 CID `multipart/related` MIME v2 与完整 RAW 核验；新增仅限日报确认后再周报的两封 Gmail
  REST 样式 canary 主机；按 A-010 收敛 Token 原子刷新与崩溃恢复，移除同 UID 对抗产生的
  CAS、逐调用 SHA 和长时间正式锁；F05 可从严格验证的 durable send capture 恢复 Gmail ID，
  即使 Gmail 改写 Message-ID 或主回执发布前崩溃也不会重发；A-014 新增 VO₂ Max 30天与
  体重14天有界最近健康快照、实际测量日期展示和修正版日报续跑，仍不接入普通运行或部署。
- M10 Gmail 投递停止继续修补 MCP，改为官方 REST：新增 Desktop OAuth、owner-only 专用 Token、
  Google官方端点门、profile失败回执、确定性 RFC822 Message-ID、每阶段最多5次只读恢复、
  单次发送、RAW 读回、canary 人工确认和崩溃后只读对账；r04/r05失败证据及旧 `unknown`
  原样保留。r07允许以 Gmail ID 和 RAW 实际 Message-ID 闭合 Provider 改写，保留已收到的
  旧标题canary，并只为剩余6份日报与1份周报建立新标题动作。r08按用户A-012授权保留
  已发送8封，再建立8个更正标题的独立请求，最终累计上限16封。
- 增加 M10 固定滚动七日 Candidate 采集、7 日报/1 周报 AI 编排、精确 Gmail 自投递请求、
  临时 `-E2E-20260818-GTS` Workout 预览及 append-only 外部动作账本；所有真实写入仍由
  对话中的精确确认门控制。早期真实释放曾因 Gmail `invalid_grant` 安全停止，失败证据永久
  保留；最终4项测试Workout完成完整生命周期并清理为残留0，官方Gmail REST闭合旧标题8封和
  更正标题8封，累计16封。最终Code、AI与Delivery/Data-Privacy Validator均PASS。
- 为 M9 日报恢复增加 Structured Outputs wire v2、递归兼容检查、公开合成 Schema canary，
  并将下游证据链版本化为 attempt 2/3 失败后唯一 attempt 4；旧 wire 与失败证据保持不变。
- 完成 M9 单日 Garmin MCP 有界只读采集与离线日报闭环：固定日期、工具、依赖和预算，使用
  cached-token-only 的离线启动器，把 MCP Capture、FIT、调用收据和离线 AI 日报仅写入
  仓库外 Candidate；Gmail、Workout、Sites、cron 与正式 state 保持关闭。
- 引入 agentForge `v0.4.2` 开发 Harness，并将 TrainLab 收敛为根 `src/`、根 `tests/`
  的唯一项目布局；移除临时 `product/`、旧开发编排控制面和无消费者的 legacy 文档。
- 将产品 Harness、Schema、策略和公开默认值迁入 wheel 的不可变
  `trainlab.resources`，以外置 `TRAINLAB_INSTANCE_ROOT` 保存私有配置、state 与 logs。
- 新增完整哈希依赖锁、可复现 wheel/runtime bundle、安全解包验证器、离线安装器和
  Linux/macOS CI 真安装链；bundle 不再包含源码 checkout、开发 Harness 或私有数据。
- 用可重复、无个人信息的代码生成 FIT 夹具替代私人测试样本，并移除项目内的
  `test_data/` 依赖与历史样本展示。
- 完成 M4 `source/` 工程、Foundation v4 离线重建与教练 Harness v2；源码 `mypy src`
  清零，补齐分析输入、SQLite、Garmin mixin 和交付工厂的显式类型边界，并保留
  tests/tools 类型债为后续候选。
