# 最终独立验证及主验收摘要

这是主Agent按原始证据整理的发布摘要，不是改写后的Validator原报告。原报告及实际结果的指纹见[acceptance.json](acceptance.json)；完整原始输入、报告、命令、日志和失败保存在原工作区`exec-plans/evidence/ADHOC-0035/validation-t04/`，不把原区业务diff/计划或安装环境完整源码夹带进本PR。

## 独立结果（2026-09-21）

全新官方Validator运行c74b772c-21d5-4136-88cd-87037222e849；原生workflow及child正式终态，进程退出0。

- 通过固定C35/Cuse的来源、实际七文件、业务保护、候选增量、角色接入及证据分层；未发现受审实现缺陷。
- 93输入、8依赖、两区39实际文件及副本首尾一致；原6825对象、38项source差异、两区原HEAD和空暂存保留。
- 正确Git子目录的origin、固定HEAD、17个普通Git blob与官方SHA一致；两区`.pi`恰好六普通文件及agents目录，旧dev/architect不在项目中。
- 37/18/9/13/27项专项断言均退出0，402个相关本地链接存在。覆盖缺/多文件、SHA、文件/目录/根符号链接、残留空目录、运行产物及近似目录名、ignore、模型后缀/覆盖/false与off、可信/非可信/子目录APPEND发现等边界。
- 官方Planner、Developer客观工具证据各自有效；本次官方Validator也实测自身模型、等级、会话及工具。均为fresh；运行中的模型/等级不写成新增固定配置。
- **独立限制T04-E01**：主会话完整有效prompt无法由该子会话独立复核。只能归因主人工观察，不能以文件、静态代码或子会话替代主实际加载证明。

## 主完整本地验收

主直接核当前有效上下文含同版官方运行约定；项目APPEND指纹、cwd和无同名全局APPEND/SYSTEM的事实相符。主加载由主直接验收，不冒充独立宿主prompt快照或reload回执。

主在另一个外置检查目录亲自运行：

- `python3 -B guard.py guard-start-corrected` / `guard-end`：首尾退出0。
- `python3 -B verify.py`：37项，退出0。
- `node config-boundaries.mjs`：18项，退出0。
- `node priority-boundaries.mjs`：9项，退出0。
- `python3 -B final-guard.py`：27项，退出0。
- Git状态NUL路径解析的7项正常/错误/边界检查符合预期。

主没有运行子会话专用自我身份断言来伪装成Validator；该部分核其真实元数据和工具事实。七文件及相关运行依赖未在独立验证后修改。后续变化只允许真实结果、状态和交付记录，并由主审差异。

## 失败与边界保留

- H35-INFRA-001：历史入口.ts/.js不匹配，失败1次；未修改安装，后续新进程同协议恢复。
- H35-INFRA-002：Planner绑定报告父目录缺失，工具失败1次；预建外置目录后全新实例正常完成，原工具退出1不被runner退出0覆盖。
- H35-CHECK-LINK-001（本轮别名T04-C01）：此前主恢复检查及本次Validator均曾误读基线链接字段。合计2次检查器错误，实际字段一直是`link`；纠正外置检查器后各自通过，未重算基线或改受审文件。
- H35-CHECK-PATH-001：主归档包含中文/空格夹具后，旧Git显示文本前缀检查误报1次；改主检查副本为`git status --porcelain=v1 --untracked-files=all -z`，仍严格限制新增收据路径和原记录保全，7项边界及完整复跑通过。
- Validator一次错误短路径读取ENOENT已保留；完整任务给出的正确路径一直存在。另有首次候选来源目录/路径别名误判，见[检查摘要](checks.md)，均未删除原失败。

以上不是受审实现修复，不清零历史计数，也不触发新的产品开发。没有未解决的受审缺陷；独立主prompt证据限制明确保留。

未验证产品、浏览器、数据库、账号、实服务、多Developer并行；本轮不要求这些且不宣称通过。ADHOC-0033继续暂停。本地适配验收后已核触发/合并条件，首次提交7faadf4已推送并创建[PR #23](https://github.com/wyizhou/TrainLab/pull/23)，仅15项本轮增量，基于未合并#22。闭合提交仅归档真实结果/链接，未改受验七文件；不自动合并，实际最新头以PR为准。
