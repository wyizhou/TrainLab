# ADHOC-0031 用户确认事项（2026-09-16）

来源：用户在当前会话回复。

- D31-01：采用主Agent建议。首次同步最近七天；新下载FIT命名为 `states/activities/YYYYMMDD-实际FIT字节SHA256.fit`，日期用UTC；未知日期 `unknown-SHA256.fit`；不混用既有64位标识命名。
- D31-02：AI私有配置已放置在 `states/ai.json`。允许发送真实请求，数据无限制，暂时不考虑请求/费用上限。密钥文件仍为私有状态文件，不进Git；开发测试可以继续使用本地模拟，真实联调按最终检查阶段记录。
- D31-03：活动总结按中国时区每日凌晨4点自动触发。允许新增 `config` 等配置表记录系统参数，便于后续Web设置接入。周总结正常依赖已有活动总结；如某一周没有任何活动，周总结返回并入库全文 `本周无任何运动记录`。多轮对话历史仅保存在内存，数据库只记录生成结果。
