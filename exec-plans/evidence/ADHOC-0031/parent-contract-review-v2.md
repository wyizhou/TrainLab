# ADHOC-0031：规划调整主审核与首轮修复范围

日期：2026-09-16。来源：[Planner调整稿](planner-v2.md)、[用户确认](user-answers-2026-09-16.md)、[首次独立验证](validator-b0-v1.md)及[主复现](parent-b0-v1-triage.json)。本文是主Agent对范围内实施细节的审核，不增加用户目标，不替代产品验证。

## 审核决定

1. 保留U31-01～06、B0～B7、0031-T1～T7、旧五功能T1～T4及全部问题编号。首次B0验收失败事实不改写，尚无修复失败轮次。
2. 采用Planner按消费者冻结合同的调整。B1可在工程底座及其消费的协议独立验收和主验收后开始；不等周触发等后续业务决定，但B0/T2及整个功能不得提前记为完成。
3. 原四张业务表列数12/4/3/3不变；按新授权增加一张config参数表。采用`key TEXT PRIMARY KEY NOT NULL`、`value_json TEXT NOT NULL`、`updated_at_utc TEXT NOT NULL`。活动和活动报告的TEXT身份主键也必须显式非空；SQLite自增ID继续由数据库生成。
4. 目录A保持：运行/共享实现物理位置在`source/skills/<skill>/scripts/`和`source/skills/_shared/scripts/`，Python导入名可为trainlab，不等于批准`source/trainlab/`作为另一实现根。包映射由Developer决定；不临时改sys.path。`source/tools/`仅放能力说明，现有检查脚本移到适用的共享检查或测试位置并保留检查覆盖。前端仍为React，不能用字符串构建壳替代；不要求本轮完成业务页面。
5. 新请求/费用许可按用户原意执行，不添加商务预算或固定8轮/16次上限。技术容量、网络超时、无进展检测与权限是另一边界：整体明确失败，不截断或保存半文。真实容量值到B5核实，不由本轮假装测得。
6. 不把私有配置文件候选键名当既定格式。本轮只固定内部适配类型，不读取实际凭据；B4/B5按授权安全核实实际文件，不要求用户重复提供可查明的信息。

## 0031-T2条款处置

以下引用Planner稿中同名条款。采用条款不等于相应产品功能已实现。公共结构在本轮落实为单一Schema/接口文档和必要校验，后续阶段承担业务执行。

| 条款 | 审核及本轮界限 | 消费者 |
| --- | --- | --- |
| /01 所有权 | 采用模块职责与单一修改者；本轮落实目录/导入检查，不提前实现所有服务 | 全部阶段 |
| /02 JSON | 采用四类容器、活动内共用字段字典、来源分离、记录时间证据、默认123和全量records返回形状；公开Schema单一来源，内部字段ID和字典引用有交叉检查。大整数/原字段bytes表示只用于可解释字段，不开放整个FIT或被排除的私人身份。新增JSON结构是实现载体，不恢复被否决的业务元数据 | B1/B2 |
| /03 时间/完整性 | 采用有时区输入、UTC固定六位小数及Z、原顺序、全链式校验、缺失不造值。零record是否合法留B1-T1依据固定SDK/协议证据判定，不预先宣布所有零record失败或成功。初次B0仅做时间与DTO校验，不能冒充FIT解析 | B1 |
| /04 身份/重复 | 采用实际字节SHA、同字节幂等、旧原件不改名、已知远端来源冲突不自动覆盖、事务重解析失败保旧。已有活动报告与改变事实冲突时停止维护，不新增重解析UI/自动补报；本轮写可执行输入结构和协议，真实仓储留B1/B3 | B1/B3/B4 |
| /05 外壳/权限接口 | 采用records唯一activity_id参数及顶层ID、三字段记录、解码metrics与共用字典、固定错误壳/映射。接口从宿主接受授权集合与视图，模型不能自行设置。普通API错误/health使用公共外壳；尚未实现路由仍404，不提前假造业务结果 | B2/B5/B6 |
| /06 报告保存 | 采用活动一条当前全文、时间取来源、空白检查不修改正文、同T可多份周报、保存重试与再调用AI分离、提交不明不盲重放。内存请求幂等只对当前进程；本轮冻结类型和协议，不提前实现生成/仓储，也不扩张聊天产品 | B3/B5 |
| /07 周窗口 | 采用`[T-7×24小时,T)`、同一T、开始归属、全文活动报告加默认123、无活动与缺报告/不可分析/输入不确定分支分开。真正空周直接保存精确文本，不调用模型。有活动缺报后的业务动作仍待用户，不把临时安全停止固化为最终策略 | B3/B5 |
| /08 config/调度 | 采用参数表白名单、单一ConfigRepository所有权、不保存秘密/messages、三小时同步与每日上海4点已确认参数、同步阶段状态分开。首次下载范围已授权；本轮不创建正式库或启动调度。活动批次/补跑/周触发不设默认 | B4/B5 |
| /09 内存历史 | 采用实例/模式/对象隔离、用户不能注入system/tool历史、完整协议顺序、重启丢失、无磁盘留存。不增加自动历史淘汰/剪裁。B0修复时间一致性及内存结构，业务Context留B5 | B5 |
| /10 AI循环 | 采用规范协议顺序、call_id关联、完整最终全文、错误/取消/无进展明确失败，不因收费轮数截断。技术检测的精确实现和检查在B5落实，保留正常多轮循环验收 | B5 |
| /11 资料 | 采用公开索引的两个reference_id、固定映射/全文/路径安全，无任意路径URL。本轮冻结参数/结果Schema，实际读取与注册在B5 | B2/B5 |
| /12 容量/一致视图 | 采用存储容量与模型发送容量分开、边界等于可接受/超限整体失败、运行内只读事务视图、单进程协调写入、普通rollback journal、无活库immutable。B0落实内部类型、输入约束和协议；数值由后续适用环境检查确认，不能把缺模型参数变成B1不能入库。采样周配额待用户决定 | B1/B2/B5 |
| /13 Web | 采用本机8080、唯一静态根、公共错误壳及不暴露秘密。首轮修复验证真实最小FastAPI应用和React生产产物；浏览搜索/报告接口、Host/Origin/XSS完整业务链由B6/B7验收，不能用B0替代 | B6/B7 |

尚未核实的SDK消息映射、设备来源或模型容量都不由Planner建议变成事实；Developer如发现承载结构不能满足全量目标，带具体证据停止受影响部分，不通过减少字段/记录迁就库。

## 当前首轮Developer修复合同

修复编号保留V31-B0-001～006；本轮不完成B1～B7。

- **001**：建立真实React最小工程及实际lint/type/test/build；生产构建直接写后端web目录。保留原壳的已确认边界覆盖，迁移时不得用空脚本换通过，不新增业务页面要求。
- **002**：项目声明包含pytest及必要测试依赖；仅用声明依赖在隔离环境运行Python3.12、pytest/Ruff/mypy/compile/Schema和最小应用检查。确保可安装/导入；不要靠Validator补装或mypy动态导入绕开应有类型检查。
- **003**：DDL及元数据非空/主外键语义一致，补正常、NULL、重复键、孤儿行和跨表保护回归。原业务列不扩张。
- **004**：用实际受控根与正规化路径阻止越界和静态根链接至私人目录；既检查校验函数，也检查真实应用factory/HTTP。普通静态文件正常提供，错误路径拒绝；仅使用合成states。不能只依赖词法parents或框架对单文件软链接的防护。
- **005**：统一拒绝无时区的绝对时间，已带偏移时间正确转UTC；历史仍只内存保存，验证不同系统时区行为一致。
- **006**：按上表落实B1需要的公共Schema/DTO、固定工具参数和结果、错误映射、时间/窗口/重复/报告保护协议、权限与技术容量内部接口、合法/非法样例及一致性检查。保存策略只冻结接口/语义，本轮不实现FIT解析、业务仓储、真实调度和AI调用。待决定的业务政策必须在文档/类型中显式标为未配置，不能用默认值假装关闭问题。完成后按实际范围报告，不能自称整个T2已交付。

同时对齐目录A和构建忽略：允许仅增补`.gitignore`精确忽略前端依赖/缓存与web生成产物，精确放行`source/uv.lock`，不放宽states或通配运行锁保护。Python/前端依赖锁随底座交付，不提交node_modules、临时环境或生成web产物。已有`source/skills/local-web/web`内未跟踪产物是构建输出，不是手写生产源码。

规范、模板和示例使用项目相对路径及通用AI表述；不要新增解释性代码注释，不顺带删除旧注释/许可。可更新产品README为实际可运行命令，角色不得修改协调计划/本主审核。证据使用合成数据；不读取states/ai.json/认证/私人FIT，不启动真实服务任务，不提交推送。修复完成后固定新快照，全新独立复验和主验收。

## 仍需用户决定的分支

此前写“D31-01～03全部闭合”过于宽泛。已经确认的命名、许可、时点、config、空周和内存历史不撤销、不重问；只有下列剩余行为暂缓：

- **D31-03A 活动批次与补跑**：4点选择上一中国自然日，还是截至本次所有未总结活动？停机错过、迟到和失败活动在启动时或下一次怎样补做？
- **D31-03B 周触发与缺报**：周报显式调用还是自动时点？有活动但缺总结时等待/报缺还是先补齐？“没有任何活动”的固定文本不适用于缺报。
- **D31-02A 采样配额**：新“不限”是否也取消旧get_running_records周累计20次；若保留，统计周的口径仍需明确。无论是否取消，本次作用域/真实running/全量/只读权限不变。

D31-03A/B只暂停相应自动选数/补跑/补报行为，D31-02A只暂停未明确的采样政策及其真实接线；工程修复和合成FIT解析不受影响。不重新要求密钥、真实请求数据集合或收费上限。
