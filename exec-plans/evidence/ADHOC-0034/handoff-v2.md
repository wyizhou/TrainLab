# 当前落地交接定义（V2）

本次仅修正主Agent交接材料；8份Harness字节及业务目标不变。固定位置以本轮`combination-v2-location.json`及派发清单为准，当前唯一机器清单为该固定根的`inputs/landing.json`。原`combination-location.json`、baseline-Cuse及首次delta作为第一轮历史材料保留，不再用它们代表当前落地前提。

- `inputs/history-baseline-Cuse/`：首次比较起点，只用于核累计业务记录增量。
- `inputs/before/`：当前落地对象的原实际字节，逐项严格匹配landing的before值；缺文件以null明确表示。现有0034执行计划、全部本轮证据也逐项列入，不以目录名代替文件清单。
- landing逐项指定目标、操作和after：`install`只允许8份H及两区记录中确有变化的已审内容；`retain`表示已有原件与候选相同，核摘要后不写入。不存在任何整目录回拷操作。
- 原175source＋4 references及6234旧证据另外按原保护清单核对；只补4个明确公开states占位/示例用于完整基线核对，不读取私人states。
- 后续独立报告、主验收收据、派发和实际Git交付证据是尚未产生的事实，不预写成功或把未知内容塞入候选。仅可在landing列明的本轮证据追加位置新建；创建时使用排他创建，已有同名文件立即停止，不覆盖既有证据。已存在证据仍由逐项retain保护。
- 通过独立及主验收后，主先验证全部before和保护项，再按清单对install项复制固定候选字节，回读核after；任何前置值变化停止，不自动覆盖或改清单。保留原分支/HEAD/38项source差异和空暂存。
- 仅真实状态/结果的后续补写及归档由主核精确差异；若改变目标、规则或影响验收的内容则重固定并新独立复验。本定义不豁免最终落地检查。
