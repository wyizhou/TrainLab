# 主Agent验收（安装前）

2026-09-20。已核V2正式workflow/child交回、50工具调用、7315固定项不变及允许新增范围。原始315项验证产物已归档，报告和首错保存；未把摘要中的通过替代实际证据。

在另一份物理副本亲跑integrity→audit→docs→guard_cases→guard_adjacent→integrity，六命令全部退出0：84项审计、17项文档检查/502链接/15语义场景、394项实际guard主场景及19项相邻场景通过。原始命令/输出在本目录commands.json及checks。guard实际读写隔离样本；仅Git和保护业务样本适配，不等于原区安装已通过。

主人工完整核AGENTS、README、角色/执行模板、技能、索引、记录增量及landing协议：规划输入按当前阶段判断；开发需已审核/接口就绪；多Developer仅各自独立目录分支，测试数据库/账号/端口也须隔离；共享项唯一归属和固定版本交接；受审范围含实际未提交及依赖、变更须重验；分别通过不代替组合验收；失败只停受影响链但不清零阈值。文档升级适用不启动浏览器；没有承诺或实际执行并行运行器/产品测试。

7份采用文件与官方37520a5原字节一致，技能索引保留项目边界，references完整；既有PLAN/MEMORY非空模板覆盖，0033仅新增隔离节，失败/原授权记录保留。126目标中的125份before与实际字节匹配，新技能null；10install/116retain与after及差异一致。全部前提先核、retain不写、追加证据排他新建；不整目录复制。

H34-V1-001/002的当前缺口经材料方法A和全新V2闭合，首次失败仍计各1。原区真实预检已单独退出0，尚未实际安装，下一步按同一冻结清单运行--apply，不能重算before迁就变化。

交付前远端复查仍37520a5/af09e3f。官方Actions工作流0、分支rules空、webhooks空；旧式保护API明确404 Branch not protected，不是认证或网络失败。没有新增/运行远端CI。主验收在本轮文档/交接范围通过；原区安装后置与Git/PR仍需真实收据。

私人实例、平台/全局配置不在本次读写范围；没有把未直接核其文件摘要表述为持续OS审计。旧业务不恢复，真实Provider调用没有发生。
