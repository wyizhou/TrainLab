# ADHOC-0034 / T02 规则情景自查

方式：2026-09-20人工完整阅读固定上游及候选H，逐项将下列输入代入规则，再核对README与角色模板是否表达相同限制。这里只验证文档语义，不启动子Agent、产品、Chrome或多Writer运行器；“通过”不代表实际并行执行成功，也不是独立验证。版本、SHA与文件范围见同目录check-results.json；行号相对项目根。

| 编号／类型 | 输入与操作 | 预期 | 实际条款及人工结论 |
| --- | --- | --- | --- |
| S01 正常 | A正在开发；B当前规划所需输入已齐、目录与资源隔离，安排Planner规划B | 允许，不必等A交付 | AGENTS.md:22-23、README.md:22允许该流水线；Planner模板:14-17要求提供输入及隔离依据。通过。 |
| S02 正常 | A开发结束，实际差异和依赖已固定；B已主审且输入、写入范围、资源齐备，验证A同时开发B | 允许，但B不得写A固定内容 | AGENTS.md:22、60、72；Developer模板:14-19与Validator模板:12-18明确版本和禁止写入边界。通过。 |
| S03 正常 | 两个独立功能各有已审核规划、独立目录/分支、唯一Writer，资源无冲突，共享项由唯一责任方交接固定版本 | 可以并行开发，最终仍须组合验收 | AGENTS.md:71-73、77与README.md:10、24、26一致。通过。 |
| S04 禁止 | 两名Developer在同目录分别改两个不同文件 | 不允许 | AGENTS.md:71明确“即使修改不同文件”；README.md:10同义，不能用分文件绕过目录限制。通过。 |
| S05 禁止 | B规划未审核，或开发必要接口仍未确定就直接开工 | 不允许，暂停当前受影响环节 | AGENTS.md:23、72分别限制规划审核及必要接口；Developer模板:14要求审核和输入依据。通过。 |
| S06 禁止 | 开发目录/分支独立，但测试竞争同一数据库、端口或账号 | 不能视为资源隔离，不得抢占验证资源 | AGENTS.md:72逐项确认资源，README.md:24明确目录隔离不足；执行计划模板:27及Developer模板:18要求记录。通过。 |
| S07 禁止 | 两功能各自修改一份同一共享Schema/接口，或没有责任方、消费版本、交接条件 | 不允许重复修改；归属不明时暂停或串行受影响任务 | AGENTS.md:73、README.md:24；三个角色模板均要求修改归属和固定版本。通过。 |
| S08 禁止 | 受审记录只有分支名，或者指向持续写入的同一目录，漏掉未提交差异 | 不算冻结，不能直接据此验证 | AGENTS.md:60、README.md:22；Validator模板:12-13要求实际未提交差异、必要文件及核对方法。通过。 |
| S09 禁止 | A/B分别通过，合在一起无文本冲突，想跳过组合验收 | 不允许 | AGENTS.md:77、README.md:26明确仍要实际组合版本、适用集成检查、独立验证及主验收。模板:29-30要求对应安排。通过。 |
| S10 边界 | B当前规划输入已齐，但一个无关的后续实现或合并尚未完成 | 不阻塞B规划；不等于B可以未审开发或已交付 | AGENTS.md:23与README.md:22按当前环节判断，保留逐功能审核/验证/交付链。通过。 |
| S11 边界 | 受审内容未改，但影响结果的依赖更新 | 旧证据不能证明新版本；停止受影响检查，重新冻结并交全新Validator | AGENTS.md:61、Validator模板:43都绑定内容和依赖，不能只核提交名。通过。 |
| S12 边界 | 共享项责任方发布新版本；消费功能原输入被影响 | 由责任方交接，消费方重核就绪，不得各自静默修改 | AGENTS.md:73明确重核；若已在验证，另受:61的重冻结与独立复验约束。通过。 |
| S13 边界 | A达到两种不同修法仍失败/连续三轮不收敛阈值；B真正独立且输入资源齐备 | 暂停A及其依赖链；B可继续；不得借并行清零A计数或自动恢复 | AGENTS.md:65、67保留累计停止线、真实用户决定与环境恢复门槛；:72不准抢占验证资源。通过。 |
| S14 边界 | 只补写真结果/状态，未改规则含义或影响结果的依赖 | 主核差异即可；若修改规则含义则必须独立复验 | AGENTS.md:61区分两类后续改动；不是任意文档变更免验。通过。 |
| S15 边界 | 本次仅升级说明和规则，不改网页行为 | 不启动浏览器；技能文件齐全不意味着实际浏览器功能验收通过 | skills/web-browser-acceptance/SKILL.md:14说明仅此Markdown；:24明确文档豁免。本次不安装依赖。通过。 |

## 结构、链接和保真人工核对

- exec-plans/template.md:22-30完整新增九字段，涵盖输入、审核、目录分支/Writer、共享归属、固定实际差异/依赖、资源、暂停链、组合责任、最终集成/独立/主验收；原任务、状态与失败记录字段保留。
- Planner模板:14-17、Developer模板:14-19、Validator模板:12-18及:43与上列规则相符，未把模板当成独立于AGENTS的新执行协议。
- README完整MIT段与上游一致；这只保留来源许可，不将项目历史产品重新许可。README的空模板说明是逐字节上游介绍，不能据此覆盖项目实际PLAN/MEMORY。
- skills/README.md仅将过期无技能段替换为上游技能链接，原项目边界段不动；references/README.md含上游说明且原两篇资料索引不动。
- 静态脚本核候选八文件及保留的references索引，共19条本地链接有效；当前这九份文档的链接没有fragment，故无待核链接锚点。已人工核关键章节存在；未遍历历史证据里的旧链接，不将它们当当前入口修补。
- 完整审阅七个既有文件的git diff及新增技能全文，未见清单外规则修改、特定AI产品表述或绝对文件系统路径。
- 374项Developer基线中367项不变；其中本worktree的116项source、4项references、主已改PLAN/执行计划/本轮协调材料及旧证据保留。该116不是原用户工作区175项source的替代验证：未访问原区，原179资源、38项差异须主Agent按T03～T05另核。

本轮未发生检查失败，无新稳定问题编号；既有H34-CHECK-001及历史失败材料未修改。C34/Cuse冻结、独立验收、主验收、原用户目录落地、交付前远端最新性与Git交付均未运行。
