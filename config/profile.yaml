schema_version: 1
timezone: Asia/Singapore

personal:
  # Month precision is sufficient for the age-based low-confidence fallback.
  birth_month: "1993-08"

goal:
  type: half_marathon
  mode: rolling_adaptive_base
  race_date: null
  target_time: "01:50:00"

running:
  # Zone definitions and safety gates live in config/running_policy.yaml.
  # Exact targets remain unavailable until automatic HR baselines are supported.
  heart_rate_method: heart_rate_reserve

strength:
  goal: running_and_climbing_support
  structure: full_body

climbing:
  planned_days: []

medical:
  constraints: []
  active_injuries: []
  heart_rate_affecting_medications: []
