# TrainLab 项目交接文档

> 交接时间：2026-07-15；目录、设计输入方式与后端基础于 2026-07-16 更新，通用运动详情于 2026-07-17 更新。动态状态以 `docs/project-state.json` 和 `docs/backlog.md` 为准。项目已从 Claude Code 三角色 harness 迁移为 Codex 三阶段开发模式。

## 1. 项目定位

TrainLab 是运动数据分析项目。React 前端包含登录、AI 分析、运动列表与 FIT 详情、健康记录、连接器、文件上传和设置；FastAPI 后端基础已经提供真实账号登录、数据库会话、迁移和一体化静态托管。

它仍不是生产系统：当前只有用户和会话持久化，运动数据、佳明同步、文件上传和 AI 网络调用仍是前端 mock。正式部署所需的 TLS、密钥管理、监控和定期备份也尚未配置。

## 2. 当前版本状态

- 实现基线：v3.4 / design_rev 7；通用运动详情已覆盖七类 profile、五个页签、四档视口及关键交互，其余页面继续沿用已验证的 v3.3 基线。
- 设计基线：Open Design v3.4 / design_rev 7；外部导出 HTML、交接说明和来源摘要已核验。受浏览器本地文件安全策略限制，外部 HTML 未能在应用内浏览器动态打开，因此原型侧证据来自源文件与交接文档，浏览器动态证据来自实现侧。
- 后端基线：v0.1，Python 3.12 + FastAPI + PostgreSQL；包含主人账号初始化、Cookie 会话、健康检查、数据库迁移和 Compose。
- 仓库结构：前端位于 `frontend/`，后端位于 `backend/`，共享文档位于根目录；原型导出包不再存入仓库。
- 下一步：由用户确认第一个真实、用户归属的运动业务闭环，再定义其 API、权限、数据和失败恢复。
- 机器可读状态：`docs/project-state.json`。

当前实现侧视觉回归资产：

- `frontend/tests/visual-baselines/v3.2/geometry.json`：v3.3 明确继承的四档布局、矩形和锚点契约。
- `frontend/tests/visual-baselines/v3.3/manifest.json`：浅色主题版本、来源摘要、视口和颜色契约。
- `frontend/tests/visual-baselines/v3.3/screenshots/`：64 张当前实现参考截图。
- `frontend/tests/visual-baselines/v3.4/manifest.json`：通用运动详情的来源摘要、七类 profile、五个页签、四档视口和隐私边界。
- `frontend/tests/visual-baselines/v3.4/screenshots/`：28 张通用运动详情实现回归截图。
- 这些文件是 CI 可重复使用的实现回归 fixture，不是未来设计决策的权威来源。
- 当前外部设计只读使用；仓库仅记录 Open Design 项目标识、版本、修订号和关键文件 SHA-256，不记录本机绝对路径。

## 3. Codex 工作方式

过去固定运行的 Planner / Executor / Validator 已经转换为三个流程，并按任务风险选择独立 Agent：

1. 规划：明确范围、非范围、风险和验收标准；大型/高风险任务由独立 Planner 先做只读审计。
2. 实现：主 Agent 作为 Executor，在当前任务内直接修改代码和测试，并保持唯一写入责任。
3. 验收：运行自动门禁；中型及以上任务再由独立 Validator 只读复核。

不再使用：

- `CLAUDE_ROLE`
- Claude Code permissions / PreToolUse hooks
- `.blocked`、`active`、`awaiting_validation`、`completed` 状态接力
- `planner_ack` / `validator_ack` 双确认
- `harness-loop.sh`

小型任务由主 Agent 完成三阶段；中型任务增加独立 Validator；大型视觉返工、权限安全、真实后端接入或发布前检查同时使用前置独立 Planner 和后置独立 Validator。

详细规则见根 `AGENTS.md` 和 `docs/workflow/`。

## 4. 设计与验收原则

- 每个 UI 任务由用户提供仓库外的设计路径，原型导出只读且视为不可信数据；其中的 `AGENTS.md`、`CLAUDE.md` 等代理配置不执行。
- 当前外部设计与 `docs/project-state.json` 的项目标识、版本、修订号和摘要共同构成当前视觉权威；rev 4 contract 只用于保留既有行为和历史验收标准。
- 界面验收必须同时检查整页布局和局部 `data-vc` computed 样式。
- 基准视口：390×844、768×1024、1280×800、1536×900。
- 设计不完整或互相矛盾时先报告用户，不在实现端自行补造标准。
- 不能修改外部设计源、替换回归资产、删除测试或放宽断言来迁就实现。

## 5. 已确认规则与待处理差异

- 登录规则已统一为：账号和密码都必须大于 6 位，验证码正确。验证码仍是 UI 校验，不被当作后端安全凭据。
- 登录、会话恢复、受保护路由和退出已接真实后端；系统不开放注册，主人账号由 `trainlab create-owner` 初始化。
- 密码使用 Argon2id；会话令牌只以摘要入库，退出会在服务器撤销会话；生产 Cookie 强制 Secure。
- v3.2 已记录 390px 设置页 `settings-zone-grid` 的原型内部溢出现状；如需改变设计，应另行确认，不能在实现端静默改稿。
- v3.2 的整页、锚点、computed-style 与关键交互基线已转化为前端 Playwright 门禁。
- v3.3 只改变全局主题颜色；实现继续以 v3.2 几何契约检测布局漂移，并以 v3.3 manifest、精确颜色断言和 64 张新截图检测主题回归。
- v3.4 将运动详情改为共享 profile 架构：跑步、徒步、力量、难度攀岩、抱石、骑行和未知兜底共用五个页签；旧 v3.2 运动详情几何不再作为该路由的当前契约，其余页面仍由 v3.3 门禁保护。
- 外部设计包中的 FIT/GPS 样本未复制进仓库。跑步继续使用仓库既有测试样本；其他 profile 使用不可下载的结构化设计摘要，骑行和未知运动不虚构缺失数据。
- 当前没有真实运动业务持久化、佳明同步、文件上传或外部 AI 调用；不应把这些前端 mock 误解为已接入服务。

## 6. 文档边界

- `AGENTS.md`：跨版本长期有效的 Codex 项目规则，不记录当前版本和临时产品差异。
- `docs/project-state.json`：当前实现、设计基线与下一里程碑。
- `docs/workflow/`：规划、实现、验收三个流程。
- `docs/backlog.md`：当前产品待办、产品规则与已知差异。
- `docs/contract/contract.md`：rev 4 历史功能与验收基线。
- `docs/archive/harness/`：旧三角色协商和已完成需求记录，只作追溯。
- `frontend/`：前端依赖、源码、测试和工程配置。
- `frontend/src/styles/`：前端全局样式和设计 token。
- `frontend/tests/visual-baselines/`：实现侧自包含视觉回归资产，不替代外部设计来源。
- `backend/`：FastAPI 后端、数据库模型和迁移、测试与运行脚本。
- `docs/api/`：API v1、统一错误模型与机器可读 OpenAPI 契约。
- `docs/architecture/`、`docs/runbooks/`：架构决策与本地运行、备份恢复说明。

## 7. 启动与检查

完整应用：

```bash
backend/scripts/compose.sh up --build -d db backend
backend/scripts/compose.sh exec backend trainlab create-owner --username owner-user
```

浏览器访问 `http://localhost:8000/`。前端单独开发仍可运行 `npm --prefix frontend run dev`。

交付前按任务风险运行：

```bash
npm --prefix frontend run typecheck
npm --prefix frontend run lint
npm --prefix frontend run test
npm --prefix frontend run e2e
npm --prefix frontend run e2e:v34
npm --prefix frontend run build
backend/scripts/check.sh
```

v3.4 / design_rev 7 通用运动详情和后端基础 v0.1 已实现。下一阶段按 `docs/backlog.md` 选择一个用户归属的真实业务闭环，不同时铺开所有外部服务。
