# Mail Agent synthetic fixtures

This directory may contain only synthetic, de-identified test inputs. Do not
place Gmail exports, real health data, OAuth credentials, tokens, complete
email bodies, or Codex transcripts here. Tests should use stable identifiers
such as `message-1` and deliberately non-routable example values.
