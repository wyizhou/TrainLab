# TrainLab Backend

该目录用于后续后端工程。当前只完成前后端目录边界划分，尚未选择后端语言、框架、数据库、认证方案或部署方式，也没有引入任何真实网络服务。

开始后端实现前，应先在根目录 `docs/backlog.md` 中确认产品范围，并明确：

- API 边界和前后端数据契约；
- 用户、认证与权限模型；
- 数据库与迁移策略；
- 佳明同步、文件处理和 AI 服务的安全边界；
- 测试、部署、监控和回滚方案。

后端技术栈确定后，在本目录增加自己的依赖清单、源码、测试、运行说明和必要的局部规则。
